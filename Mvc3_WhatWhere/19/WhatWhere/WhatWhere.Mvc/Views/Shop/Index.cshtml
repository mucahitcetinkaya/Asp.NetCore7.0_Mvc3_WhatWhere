@*@model List<Shop>

<div class="container">
    <div class="row row-cols-1 row-cols-md-6">
        @foreach (var shop in Model)
        {
            <div class="col mb-4">
                <div class="card">
                    <img src="~/images/shop/@shop.ImageUrl" class="card-img-top" alt="...">
                    <div class="card-body d-flex flex-column justify-content-between" style="height: 200px;">
                        <h6 class="card-title">@shop.Name</h6>
                        <h6 class="card-title">@shop.CityId</h6>
                        <h6 class="card-title">@shop.TownId</h6>
                        <h6 class="card-text">@shop.PhoneNumber</h6>
                    </div>
                    <div class="card-footer bg-white d-grid gap-2 mt-2">
                        <button href="#" class="btn btn-success btn-sm">Detaylar</button>
                    </div>
                </div>
            </div>
        }
    </div>
</div>*@

@model List<Shop>

<div class="container">
    <div class="row row-cols-1 row-cols-md-6">
        @foreach (var shop in Model)
        {
            <div class="col mb-4">
                <div class="card">
                    <img src="~/images/shop/@shop.ImageUrl" class="card-img-top" alt="...">
                    <div class="card-body d-flex flex-column justify-content-between" style="height: 200px;">
                        <h6 class="card-title">@shop.Name</h6>
                        <h6 class="card-title">@shop.City.Sehir</h6>
                        <h6 class="card-title">@shop.Town.Ilce</h6>
                        <h6 class="card-text">@shop.PhoneNumber</h6>
                    </div>
                    <div class="card-footer bg-white d-grid gap-2 mt-2">
                        <button href="#" class="btn btn-success btn-sm">Detaylar</button>
                    </div>
                </div>
            </div>
        }
    </div>
</div>