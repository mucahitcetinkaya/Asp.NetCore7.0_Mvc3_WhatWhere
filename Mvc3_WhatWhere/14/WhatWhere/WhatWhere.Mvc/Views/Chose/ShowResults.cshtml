@model List<Shop>

<h2>Mağaza Sonuçları</h2>

@if (Model.Count == 0)
{
    <p>Mağaza bulunamadı.</p>
}
else
{
    <ul>
        @foreach (var shop in Model)
        {
            <li>@shop.Name</li>
        }
    </ul>
}
